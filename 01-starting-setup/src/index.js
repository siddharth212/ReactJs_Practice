import ReactDOM from 'react-dom/client';

import './index.css';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);


//Notes - 
// event.preventDefault() - its use to prevent the default behavious of sending request and reloading page on submit button click
